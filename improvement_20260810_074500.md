# CSVG Pipeline — Improvements & Review Notes (Revision-loop convergence)

- **Generated:** 2026-08-10 07:45 UTC
- **Head:** `baf6985` (`feat(quality): converge script revision loop + A2A alignment + hermetic E2E`)
- **Verified on:** run `csvg-exec-20260809-194005` (hybrid RAG, topic "60,000-year-old ostrich eggshell engravings…")
- **Scope:** revision-loop convergence review + upstream `finish_reason=error` behavior

---

## 1. Observer revision loop: now converging (was growing)

Observed violation count across the bounded 3-revision loop:

| Attempt | Violations | Class |
|---|---|---|
| initial | 25 | mixed |
| 1 | 14 | ↓ |
| 2 | 9 | ↓ |
| 3 | 8 (soft-approved) | all style-only |

**Outcome:** monotonic decrease, **0 hard violations**, script accepted via soft approval
(hard=0, score 0.693), pipeline proceeded to `PHASE_3_MEDIA_PRODUCTION`.

**Why it's fixed (commit `baf6985`):**
- Surgical per-shot repair replaces full 18-shot re-generation. Non-failing shots stay
  bit-identical, so counts converge instead of growing (old trajectory: 25→27→32).
- `orchestrator.py` keeps the **best draft** by `(hard_count, -paraphrase_diversity)`,
  then soft-approves after 3 revisions when only style-class violations remain
  (`ALLOW_SOFT_APPROVAL=1` default). Hard/fact/temporal/revenue/audience violations still abort.

**Findings — no regressions:** the 8 remaining soft violations are expected:
- Shot #16 verbatim (sim 0.98) is a **direct quote with protected proper nouns**
  (names/orgs), so WordNet dissolve correctly cannot rephrase it. Inherent tension —
  fact-dense attributions can't drop below ~0.94 without losing facts.
- Shot #17 verbatim (sim 0.95) sits just above the 0.94 bar; the dissolve ran
  (evidence: `capableness` substitution) but exhausted its 8-synonym budget.

---

## 2. Upstream `finish_reason=error` — behavior confirmed (not a local bug)

- `src/engine/llm_client.py:290` only **relays** an upstream signal: OpenRouter sets
  `finish_reason="error"` (and/or `choice.error`) when the provider (Google Gemini
  serving `google/gemini-2.5-flash`) fails mid-generation.
- OpenRouter returns HTTP 200 with the error inside the JSON body, so it cannot be
  caught via `status_code` — it must be inspected on `choice`.
- The client treats it as **transient**: retries up to 3× (backoff 2s/4s/6s), then
  falls through the model chain. All calls this run eventually recovered on try 2/3.
- If persistent: switch primary model or check OpenRouter/Gemini status — not a code defect.

---

## 3. Minor polish opportunity (not a bug)

`story_designer.py:_dissolve_verbatim_copies` (`_best_synonym`, line ~584) can emit
**stilted WordNet substitutions** — e.g. *"The capableness to transform simple
forms…"* (ability → capableness). Semantically valid, stylistically awkward.

**Potential follow-up:** add a synonym-quality guard in `_best_synonym` (word-frequency
cutoff / length cap / reject hyphenated or overly-formal alternates) so dissolves read
more naturally without broadening the budget's side effects. Optional; no correctness impact.

---

## Key outcomes

1. Revision loop converges (25→14→9→8, hard=0) — the growing-loop defect is resolved.
2. Soft approval is firing correctly and guarding the hard invariants.
3. `finish_reason=error` is upstream Gemini flakiness, correctly retried + fallback.
