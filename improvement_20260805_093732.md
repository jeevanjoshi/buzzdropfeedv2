# CSVG Pipeline — Improvements & Fixes Log

- **Generated:** 2026-08-05 09:37 UTC
- **Baseline:** reverted to `4c83941` content (branch synced to remote at `f619be1`)
- **Head:** `f1d612b`
- **Scope:** 24 improvement/fix commits since the revert baseline
- **Files changed:** 15 files, +1,638 / −537 lines

---

## 1. Data Retrieval — FactRetriever / RSS / TOPSIS

| Commit | Change | Files |
|---|---|---|
| `f387d69` | **Real RSS scores** — TVS from real feed recency (48h freshness half-life) + cross-feed coverage, SDI from real NLTK VADER sentiment (bounded download, neutral fallback); removed hardcoded fixtures | `rss_ingestion.py`, `story_designer.py`, `llm_client.py` |
| `c46a523` | **Real competitor view demand** — new `youtube_topic_demand` engine + net-RPM math + cross-run novelty | `youtube_topic_demand.py` (new), `rss_ingestion.py`, `monetization_optimizer.py`, `fact_retriever.py`, `state.py` |
| `7f397dc` | **Recalibrate niche net-RPM matrix** to Q2 2026 benchmarks; unify matrix keys with RSS category names (fixes silent fallback bug) | `monetization_optimizer.py` |
| `553c53f` | **Route high-value Legal & Real Estate niches** (signals + niche map + RPM taxonomy) | `rss_ingestion.py`, `text_embeddings.py` |
| `e1dda2a` | **Locale & seasonal RPM multipliers** in revenue forecast | `monetization_optimizer.py`, `fact_retriever.py` |
| `af8d78b` | **YouTube search quota budget** (daily, persistent) + structured logging (no stdout spam) | `youtube_topic_demand.py` |
| `e608cc6` | **YouTube API key rotation** on 429/quota/403 (primary + fallback) | `youtube_topic_demand.py` |
| `eb1cd8e` | **Third YouTube API fallback key** (`YOUTUBE_API_KEY_FALLBACK2`) | `youtube_topic_demand.py` |
| `6a5fe1e` | **Niche video-ID pool + batch `videos.list`** — minimize scarce `search.list`, use high-limit batch stats | `youtube_topic_demand.py`, `rss_ingestion.py` |
| `0241c47` | **High-RPM topic preference** (prefer specialized niches over general even in GROWTH) | `fact_retriever.py` |

---

## 2. RAG — `rag_retriever.py`

| Commit | Change |
|---|---|
| `6f52fa6` | **RAG cache** (6h TTL) + unified category (prefer RSS niche) + filtered graph triplets |
| `2204434` | **Tavily + Firecrawl sources wired in; DuckDuckGo HTML scraper removed** (ad-corrupting); expose `fact_corpus` → Observer |
| `1186d2a` | **Recency-aware RAG** — tag snippets (recent vs historical) + temporal-grounding rule in prompt |

---

## 3. Story Design — `story_designer.py`

| Commit | Change |
|---|---|
| `f387d69` | **Robust LLM script retry** (max 3 attempts, repair hint, abort — no silent template fallback) + honest `llm_mode` + `max_tokens` |
| `7f9d20d` | **LLM editor polish pass** (fact-preserving rewrite) + **removed boilerplate template fallback entirely** |
| `25de35c` | **High-CTR titles (LLM, safe fallback)** + trusted-source narration padding (drop DuckDuckGo snippets) |
| `a53b674` | **Resilient polish (retry) + strip raw `[Tavily/Exa/...]` tags & `[...]` artifacts** from narration |
| `0241c47` | **Verbatim web-quote removal** (`Hi all!` / `ICYMI` / `Log in` / etc.) in `_clean_narration` |
| `af8d78b` | **LLM retry backoff** between script/polish attempts |
| `f1d612b` | **LLM client-level transient retry + fallback model chain** (`deepseek/deepseek-v4-flash-0731`) |

**Files touched by stage:** `story_designer.py`, `llm_client.py`, `rag_retriever.py`

---

## 4. Observer — audit / anti-slop

| Commit | Change |
|---|---|
| `fcf72f4` | **Observer anti-slop + source-diversity + creative-English tolerance** (sentence duplication, citation diversity, flexible style) |
| `3a6d28f` | **Replace hardcoded creative-marker heuristic with LLM critic judgment** (STYLE vs ASSERTION classification) |
| `1186d2a` | **Temporal hardening** — route present-tense past-year claims to AI critic; temporal rule in critic |
| `0241c47` | **Single-keyword over-repetition check** across shots |
| `a3995cc` | **Fix: exclude topic's own keywords** from over-repetition (prevents infinite revision loops) |

**Files:** `observer.py`, `state.py`

---

## 5. Media Production — `media_producer.py`

| Commit | Change |
|---|---|
| `ca0e303` | **Budget-guarded media generation** — `media_budget.py` monthly paid-image cap (~₹2000/mo) + premium Flux shots {1,8,12,15} + free-assets-first + economy switch |
| `afb8d72` | **Auto-cut Shorts clips** (ffmpeg, vertical 1080x1920) + **CTR-optimized thumbnail brief** (fixed bug: thumbnail was reading a non-existent field) |

**Files:** `media_producer.py`, `media_budget.py` (new), `state.py`

---

## 6. Publishing — `publisher.py`

| Commit | Change |
|---|---|
| `71ed603` | **Publish with StoryDesigner SEO metadata** (high-CTR title, full description, tags) |

---

## 7. Schema — `schemas/state.py`

- `dd5ec4a` — **Fix: add missing `crawled_content` field** to `GlobalState` (RAG fact corpus bucket; was crashing StoryDesigner/Observer at this baseline)
- Added `shorts` list to `AssetPaths`, `competitor_30d_avg_views` to `TopicCandidate`

---

## 8. New files & runtime artifacts

| File | Purpose |
|---|---|
| `src/engine/youtube_topic_demand.py` | Competitor view-demand engine (reference CPU) |
| `src/engine/media_budget.py` | Monthly paid-image budget guard |
| `yt_demand_pools.json` | Persistent per-niche competitor video-ID pools |
| `yt_demand_quota.json` | Persistent daily YouTube search-quota counter |
| `.env` (gitignored) | Added `YOUTUBE_API_KEY` + 2 fallback keys, `LLM_FALLBACK_MODEL` |

> note: `.env` and runtime JSON state files are gitignored/local; the committed code reads them via `os.getenv`.

---

## Key behavioral outcomes

1. **No boilerplate fallback** — Live LLM is required; on total failure the run aborts (fails hard) instead of emitting template content.
2. **Creativity + fact-grounding balanced** — LLM critic is the AI judge for style-vs-assertion; RAG recency-tagged & promo-filtered; narration stripped of tool-tags/web-quotes.
3. **Quota-resilient** — YouTube search budget + 3-key rotation + batch-stats endpoint; LLM retry/backoff + DeepSeek fallback chain.
4. **Budget-protected** — paid images capped (~₹2000/mo), free assets first.
